name: Cypress tests dashboasrd
on: [push]
jobs:
  cypress-run:
    name: Cypress run
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Cypress run
        uses: cypress-io/github-action@v4
        with:
          record: true
        env:
          # pass the Dashboard record key as an environment variable
          CYPRESS_RECORD_KEY: 43a964e9-e4a1-4a76-8b5b-09e310006655
          # pass GitHub token to allow accurately detecting a build vs a re-run build
          GITHUB_TOKEN: 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
